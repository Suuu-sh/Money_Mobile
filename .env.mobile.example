# Copy this file to .env.mobile and set your local/test credentials.
# This file is ignored by Git. The values below are placeholders.

# Enable automatic login when the app launches (for local testing only).
AUTO_LOGIN_ENABLED=true

# Credentials used for automatic login in test environments.
TEST_LOGIN_EMAIL=your-test-user@example.com
TEST_LOGIN_PASSWORD=your-strong-password
